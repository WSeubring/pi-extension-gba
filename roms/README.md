# ROM folder

Drop your legally-obtained `.gba` files here. The extension scans this
directory on startup.

This folder is gitignored — ROMs are never committed to the repo.

The default path the extension looks at is `~/.config/pi/roms/gba/`. This
in-repo folder is for local development only.
